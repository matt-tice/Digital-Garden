:PROPERTIES:
:ID:       5d61ba50-722e-4e1d-b109-9f5236c5174e
:END:
#+title: emacs
#+filetags: Notes

* References 

* Summary 

In this file I'll record some of the things I've learned about emacs usage --- tips, tricks, debugging, etc.



* Rectangles

* Registers

They store things:
+ Text
+ Rectangles
+ Buffer names
+ Frame configs
+ File names

Each register has a *single letter* as its name

** Bookmarks

Lets you jump to specific files or directories very quickly
It saves them forever, unlike registers


* Latex

Auctex has two different (similar) modes:
- latex-mode
- tex-mode

As it turns out, you *need both of them*
- tex-mode is the thing that lets you compile and such
- latex-mode

* Keybindings

You can remove keybindings with

global-unset-key
local-unset-key

Similarly, you can set them with

global-set-key
local-set-key


* Org-Agenda

You can *add agenda files* by going to the relevent file and typing
/M-x org-agenda-file-to-front/
- =C-c [=
And remove them with /M-x org-remove-file/
-=C-c ]=

      
* Emms

Ok so I tried for a while to get this thing working, but upon restarting my emacs, I realized I'd somehow messed everything up. So I'm just going to *remove emms for now*

*** Old

A music player for emacs (this thing really has it all!)

:TO-FIX:
I can't figure out how to get it working on Windows
I tried getting it to work with VLC, but for some reason it looks like it's playing, but has no audio, and the songs just seem to crash and skip very quickly

So now I'm to get MPV working
But the problem is it seems that MPV wants to use something called a *socket* while Windows uses *pipes*

:END:





* EWW

Emacs has a web browser! That's crazy

It doesn't allow javascript or most CSS, so it's actually pretty safe

It actually looks pretty good --- it's basically just the way elfeed looks



* [[id:287cc124-247a-4e24-a260-eb5be60d02c6][AUCteX]]
